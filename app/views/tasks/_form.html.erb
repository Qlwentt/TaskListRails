<%= form_for @my_task, url: @path do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>

	<%= f.label :description %>
	<%= f.text_field :description %>

<!-- 	<%= f.label :completed_at %>
	<%= f.text_area :completed_at %> -->

	<%= f.submit %>
<% end %>



